extends layout

block content
  .container(class="d-flex flex-wrap justify-content-center py-3" style="gap: 4rem;")
      button(class="btn btn-primary") Food 
      button(class="btn btn-primary") Drinks
